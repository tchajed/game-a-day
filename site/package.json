{
  "name": "game-a-day-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate": "bun scripts/generate.ts",
    "dev": "bun run generate && vite",
    "build": "bun scripts/build.ts",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "sharp": "latest",
    "typescript": "latest",
    "vite": "latest"
  }
}
