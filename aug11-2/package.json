{
  "name": "museum-of-ordinary-things",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "bun run build && playwright test",
    "art:night-garden": "node scripts/generate-night-garden.mjs --all"
  },
  "dependencies": {
    "playcanvas": "^2.21.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  }
}
